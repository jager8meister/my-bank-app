apiVersion: v1
kind: Pod
metadata:
  name: {{ include "auth-service.fullname" . }}-test-health
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  containers:
    - name: test
      image: busybox:1.36
      command: ['wget', '-qO-', '--timeout=30', 'http://auth-service:8083/actuator/health']
  restartPolicy: Never
