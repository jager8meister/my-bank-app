apiVersion: v1
kind: Service
metadata:
  name: transfer-service
  labels:
    app: transfer-service
spec:
  type: ClusterIP
  ports:
    - port: 8085
      targetPort: 8085
      name: http
  selector:
    app: transfer-service
