apiVersion: v1
kind: Secret
metadata:
  name: {{ include "cash-service.fullname" . }}-secret
  labels:
    app: cash-service
type: Opaque
stringData:
  MICROSERVICES_CLIENT_SECRET: {{ .Values.global.microservicesClientSecret | quote }}
