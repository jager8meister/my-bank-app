apiVersion: v1
kind: Service
metadata:
  name: front-service
  labels:
    app: front-service
spec:
  type: LoadBalancer
  ports:
    - port: 8084
      targetPort: 8084
      name: http
  selector:
    app: front-service
