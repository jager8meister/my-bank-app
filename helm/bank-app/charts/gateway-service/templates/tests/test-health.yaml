apiVersion: v1
kind: Pod
metadata:
  name: {{ include "gateway-service.fullname" . }}-test-health
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  containers:
    - name: test
      image: busybox:1.36
      command: ['wget', '-qO-', '--timeout=30', 'http://gateway-service:8080/actuator/health']
  restartPolicy: Never
